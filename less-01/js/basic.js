"use strict";

/**
 * Конвертируем температуру по Цельсию в температуру по Фаренгейту
 * @param tc
 */
function convertTc2Tf(tc) {
    let tf = (9 / 5) * tc + 32;
    tf.toFixed(0);
    return tf;
}

function getVal(value) {
    let name = 'Василий';
    let admin = name;
    return admin;
}

function getVal2() {
    return 1000 + "108";
}